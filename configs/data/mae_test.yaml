_target_: src.data.edf_datamodule.EDFTESTDataModule
data_dir: "/itet-stor/schepasc/deepeye_storage/tueg/edf/000"
batch_size: 16 # Needs to be divisible by the number of devices (e.g., if in a distributed setup)
num_workers: 0
pin_memory: False