_target_: src.data.edf_datamodule.EDFDataModule
data_dir: "/home/schepasc/eeg-foundation/src/data/debug_json"
batch_size: 4 # Needs to be divisible by the number of devices (e.g., if in a distributed setup)
num_workers: 1
pin_memory: False